map $http_upgrade $connection_upgrade {
    default upgrade;
    '' close;
}

# API 速率限制：每个 IP 每秒 30 个请求，突发 60
limit_req_zone $binary_remote_addr zone=api_limit:10m rate=30r/s;

# 登录/注册速率限制：每个 IP 每秒 5 个请求
limit_req_zone $binary_remote_addr zone=auth_limit:5m rate=5r/s;

# 上游连接池
upstream backend {
    server api:8888;
    keepalive 32;
    keepalive_requests 1000;
    # keepalive_timeout 移除：WebSocket 长连接不应被上游超时断开
    # 默认值 60s 在 WebSocket 场景下可能导致连接意外关闭
}

server {
    listen 80;
    server_name localhost;
    root /usr/share/nginx/html;
    index index.html;
    client_max_body_size 10M;

    # gzip 压缩
    gzip on;
    gzip_vary on;
    gzip_proxied any;
    gzip_comp_level 4;
    gzip_min_length 256;
    gzip_types
        text/plain
        text/css
        text/javascript
        application/javascript
        application/json
        application/xml
        image/svg+xml;

    # 安全头
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;

    # 登录/注册接口使用更严格的速率限制
    location ~ ^/api/v1/auth/(login|register|send-verify-code) {
        limit_req zone=auth_limit burst=10 nodelay;

        proxy_pass http://backend;
        proxy_set_header Host $http_host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Host $http_host;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Forwarded-Port $server_port;
        proxy_http_version 1.1;
        proxy_set_header Connection "";
        proxy_connect_timeout 30s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }

    # WebSocket 代理（Agent 反向连接 / SSH 终端 / Exec 终端）
    # 必须放在通用 /api/ location 之前，nginx 对正则 location 按首次匹配优先
    # 关键：关闭代理缓冲（proxy_buffering off），WebSocket 帧必须实时透传
    location ~ ^/api/v1/ws/ {
        proxy_pass http://backend;
        proxy_set_header Host $http_host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header REMOTE-HOST $remote_addr;
        proxy_set_header X-Forwarded-Host $http_host;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Forwarded-Port $server_port;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;
        proxy_http_version 1.1;
        proxy_connect_timeout 60s;
        proxy_send_timeout 3600s;
        proxy_read_timeout 3600s;
        proxy_buffering off;
    }

    # API 和 Swagger
    location ~ ^/(api|swagger)/ {
        limit_req zone=api_limit burst=60 nodelay;

        proxy_pass http://backend;
        proxy_set_header Host $http_host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header REMOTE-HOST $remote_addr;
        proxy_set_header X-Forwarded-Host $http_host;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Forwarded-Port $server_port;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;
        proxy_http_version 1.1;
        proxy_connect_timeout 60s;
        proxy_send_timeout 600s;
        proxy_read_timeout 600s;
        proxy_buffering on;
        proxy_buffer_size 8k;
        proxy_buffers 8 16k;
        proxy_busy_buffers_size 32k;
    }

    # 静态资源（长缓存）
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        expires 7d;
        add_header Cache-Control "public, immutable";
        try_files $uri =404;
    }

    # 前端路由
    location / {
        try_files $uri $uri/ /index.html;
        # index.html 不缓存，确保获取最新版本
        location = /index.html {
            add_header Cache-Control "no-cache";
        }
    }
}
